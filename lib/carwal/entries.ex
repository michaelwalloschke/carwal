defmodule CarWal.Entries do
  @moduledoc """
  Context owning `entries` and `entry_revisions` (AD-1) — the unified
  entry model (FR4). Will include the `CarWal.Entries.Suggestions`
  submodule (AD-9). Schema lands in Story 2.2 (AD-12 closed enums).

  All reads are visibility-scoped per AD-10.
  """
end

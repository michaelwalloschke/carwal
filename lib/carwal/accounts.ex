defmodule CarWal.Accounts do
  @moduledoc """
  Context for users and the family membership seed (AD-1).

  Story 1.2 generates the real implementation via `mix phx.gen.auth`
  (magic-link login); overwriting this file there is expected.
  """
end

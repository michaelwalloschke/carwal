---
scope: CarWal whole system (greenfield)
purpose: build substrate for epics/stories + bmad-loop agents
altitude: initiative/system
updated: 2026-07-05T23:16
---

- (decision) Realtime topology: everything over LiveView (streams+PubSub for chat, Presence+JS geolocation hook for GPS); no separate Phoenix Channels/custom channel JS. Prevents two realtime programming models. Supersedes addendum's literal 'Channels' wording.
- (decision) Context map: 6 Phoenix contexts — Accounts, Entries (owns entry+entry_revisions+visibility, Suggestions submodule with LLMProvider behaviour), Ingestion (pollers+whitelist, writes only via Entries API), Chat (owns messages), Location (ephemeral, Presence), Notifications (owns push_subscriptions, routing defaults, digest, Oban). One owner per table. Prevents second write path on entry and cross-context Repo access.
- (version) Web-verified 2026-07: Elixir 1.20.x/OTP27+, Phoenix 1.8.8 (phx.new starter: Bandit, Tailwind4+daisyUI5, LiveView 1.2.x, magic-link auth default in phx.gen.auth), Oban 2.23 OSS (Cron plugin), libs: ical ~1.1, yugo 1.0 (IMAP), ex_nudge 1.0 (VAPID), mistral 0.5 (Req-based)
- (constraint) Daughter's Android runs Google Family Link (supervised account) — PWA install, web push permission, geolocation access and downtime windows may be restricted; needs verification on real device before onboarding (user-raised 2026-07-05)
- (decision) Coupling: business logic via direct context function calls (Ingestion->Entries.upsert_from_feed, Entries->Notifications.notify explicit); Phoenix.PubSub ONLY for UI updates (owner context broadcasts after commit, LiveViews subscribe). No domain-event bus, no Oban as coupling layer. Prevents invisible who-listens-to-what behavior.
- (constraint) Family Link verified (web, 2026-07): PWA installable without parent approval; push+geolocation need one-time 'Permissions for sites' ON -> grant -> grants persist; downtime locks device (pushes silenced); Chrome app timer also blocks WebAPK PWAs. Onboarding runbook: whitelist domain, one-time permission ritual on daughter's device. TWA via Play private track = P2 fallback if it chafes.
- (decision) Recurrence: materialize occurrences as individual entry rows within rolling ~14-month horizon (pollers expand RRULEs, seed expands birthdays, Oban extends horizon). RRULE logic lives only in Ingestion/seed; calendar/digest/reminder read flat rows. Prevents triple occurrence math.
- (decision) ADOPTED conventions (obvious defaults, not coached): phx.new 1.8.8 starter as-is (Bandit, Tailwind4+daisyUI5, LiveView, magic-link phx.gen.auth, scopes pattern for auth-scoped access); web layer never touches Repo/Ecto schemas directly, context APIs only; timestamps stored UTC, rendered+scheduled in Europe/Berlin wall time; ingestion idempotency via upsert on (source, external_uid, occurrence_date) with entry_revisions diff on change; media via storage behaviour + authenticated Plug controller; gettext German default; deployment/backup envelope adopted from PRD addendum unchanged
- (decision) Gate fixes applied: AD-12 (closed entry enums + status transition ownership, partial unique index on ingested/seed sources), AD-13 (single push pipeline per-device (user_id,endpoint), one live_session/shell, canonical PubSub topics), AD-7 tightened (schedule_reminder contract, no sweep), AD-10 two-tier reads (system reads enumerated, digest recipient-scoped), AD-5 (seed writes config only, birthdays via Ingestion source :seed), AD-6 (+no-auto-dedup/manual parent_id link), AD-9 (+volume guard, accept->family, German output), AD-4 (+one chat room)
- (version) Corrections from version reviewer: ical ~>2.0 (was 1.1), PostgreSQL 18.x (was 16), Swoosh SMTP via German sovereign mailbox added to stack+conventions; niche single-maintainer deps (ical, yugo, mistral) accepted — behaviour seams contain the risk
- (event) spine finalized
- (version) Spine update (party review of stories): Leaflet 1.9.x pinned in Stack for the OSM map — prevents Leaflet-vs-MapLibre divergence between agents; AD ids unchanged
